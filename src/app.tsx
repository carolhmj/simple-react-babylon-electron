import * as React from "react";
import * as ReactDOM from "react-dom";
import { BabylonRenderer } from "./babylonRenderer";

function render() {
  ReactDOM.render(<BabylonRenderer />, document.body);
}

render();
